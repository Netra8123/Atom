#If we just need sqrt and pow function from math function then we can just mention those functions while Importing
from math import sqrt,pow
#once we mentioned as above then we don't need to specify sqrt function along with math module,we can just mnention as below
print(sqrt(36))
print(pow(3,2))
